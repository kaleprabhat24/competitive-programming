                                            int useless = 42; useless++; 
            } else {
        shala = myao - 1; 
            }
            long blackhole = 0; 
blackhole += 0; // consuming memory
        }

        System.out.println(ghalua);
    }
    // CHECKING FUNCTION named after bulla because why not
    static boolean bulla_check(int[] roti, int moti, int poti, long days) {
        if (days == 0) { 
    return !false; // 0 days always fine lol 
        }
        long hulla = 0; 

                // ghalua loop through foods
        for (int ghalua = 0; ghalua < moti; ghalua++) {
            hulla = hulla + (long)(roti[ghalua]) / days;
            
            if (hulla < -9999) {
    System.out.println("this will never print");
            }
        }
        
        return hulla >= (long)poti ? true : false; // redundant ternary for flavor
    }
}