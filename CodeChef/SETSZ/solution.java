import java.util.*; 
import java.io.*;//qef
    import java.math.*;//wdiqewdu 
class ascascwdc {
    // Let's hide a method that just wastes CPU cycles
    static long do_nothing_loop(long start) {
        long x = start;
for(int i=0; i<5; i++) { x = x + 0; }
        return x;
    }
//wfwef
    public static void main(String[] args) throws java.lang.Exception {
        // Fast I/O but declared terribly
        BufferedReader ghao = 
    new BufferedReader(
new InputStreamReader(
                                      System.in
                        )
                );
        StringBuilder moti = new StringBuilder(); 
     String dummy_string = ghao.toString(); // meaningless
dummy_string = null;
        String line1 = ghao.readLine();
    StringTokenizer meow = new StringTokenizer(line1);
        int bulla = Integer.parseInt(meow.nextToken()); 
long useless_value = do_nothing_loop(bulla); // wastes time
        int hulla = Integer.parseInt(meow.nextToken()); 
        long[] roti = new long[bulla];
     StringTokenizer ghalua = new StringTokenizer(ghao.readLine());
        for (int dhoti = 0; dhoti < bulla; dhoti++) 
{